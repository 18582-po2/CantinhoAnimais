<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cantinhodosanimais">
    <!-- The following 3 used for MAPS -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_cantinhodosanimais"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_cantinhodosanimais"
        android:supportsRtl="true"
        android:theme="@style/Theme.CantinhoDosAnimais">
        <activity android:name=".View.OnlySeeAnimalActivity"></activity>
        <activity android:name="com.example.cantinhodosanimais.View.SeeAdoptionAdmin" /> <!-- The following 2 lines used for MAPS -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAnh6h5vlaSEofk40uNMm_2hp_7htA6mow" /> <!-- API KEY above -->
        <!-- <fragment android:name=".AboutActivity"></activity> -->
        <activity android:name=".View.MainContainerAdminActivity" />
        <activity android:name=".View.MainContainerUserActivity" />
        <!--
<fragment android:name=".UserAdoptionsActivity" />
        <fragment android:name=".AdminAdoptionsActivity" />
        <fragment android:name=".MainAdminActivity" />
        -->
        <activity android:name=".View.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.AdoptionQuestionnaireActivity" />
        <activity android:name=".View.SeeAnimalActivity" />
        <!--
 <fragment android:name=".AddAnimalActivity" />
         <fragment android:name=".MainUserActivity" />
        -->
        <!-- 45 -->
        <activity android:name=".View.SignUpActivity" />
        <activity android:name=".View.MapActivity" /> <!-- used for MAPS -->
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>